{
  "case_id": "tcr-02-multiple-files",
  "suite": "tapestry-category-routing",
  "scoring": {
    "outcome_weight": 0.7,
    "per_expectation_weight": 0.3,
    "required": true,
    "notes": "Fairness/alignment: all three files are DST client web UI code covered by the `client-frontend` category description and trigger. Several files in the same category domain should not dilute routing confidence — score `shuttle-client-frontend` as exact-category-match (1.0). The rationale should note that all three files belong to the same category domain. Penalise generic `shuttle` routing when a clear category match exists."
  }
}
