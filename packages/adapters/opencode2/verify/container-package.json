{
  "name": "opencode2-verify-harness",
  "private": true,
  "version": "0.0.0",
  "description": "Ephemeral, V2-only container manifest for packages/adapters/opencode2/verify (Task E1). Pins the same four V2 SDK packages as packages/adapters/opencode2/package.json and links the pre-built adapter via a file: dependency. Must never depend on the V1 opencode package.",
  "dependencies": {
    "@opencode/cli": "2.0.16",
    "@opencode/plugin": "2.0.16",
    "@opencode/sdk": "2.0.16",
    "@opencode/client": "2.0.16",
    "mustache": "^4.2.0",
    "neverthrow": "^8.2.0",
    "zod": "^4.4.3",
    "@weaveio/weave-adapter-opencode2": "file:./adapter"
  },
  "trustedDependencies": [
    "@opencode/cli"
  ]
}
