---
/**
 * MobileMenuToggle override — intentionally empty.
 *
 * The prototype docs shell has no floating Starlight hamburger button; on
 * narrow viewports the prototype simply hides the sidebar (docs.css). Rendering
 * nothing removes Starlight's fixed circular menu button and its associated
 * `starlight-menu-button` custom element / styling.
 */
---
