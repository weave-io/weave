# Weave Claude Code Adapter

`@weaveio/weave-adapter-claude-code` is an experimental artifact released only
to the nightly channel. Most Claude Code users should use the adapter bundled
with `@weaveio/weave-cli` through `weave compose --adapter claude-code`; this
standalone package exists for nightly adapter evaluation.
