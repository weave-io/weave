{
  "case_id": "tcr-04-no-match",
  "suite": "tapestry-category-routing",
  "scoring": {
    "outcome_weight": 0.6,
    "per_expectation_weight": 0.4,
    "required": true,
    "notes": "Fairness/alignment: `src/Infrastructure/Logging/Logger.cs` is infrastructure logging work and does not belong to the only declared category domain, `client-frontend`. The model should fall back to the generic `shuttle` agent (or `loom` as acceptable alternate). Credit `loom` as an accepted alternate since no category shuttle is appropriate. Do not credit `shuttle-client-frontend` — routing an infra file to the client-frontend shuttle is a clear mismatch. The rationale should explain that no category domain matched."
  }
}
