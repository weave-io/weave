---
/**
 * LanguageSelect override — intentionally empty.
 *
 * The docs site is single-locale and the prototype has no language switcher,
 * so Starlight's default `<starlight-lang-select>` must not render. Emitting
 * nothing removes the default language picker and its styling.
 */
---
