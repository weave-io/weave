{
  "case_id": "pattern-plan-release-checklist",
  "suite": "pattern-planning",
  "scoring": {
    "outcome_weight": 0.7,
    "per_expectation_weight": 0.3,
    "required": true,
    "notes": "Fairness/alignment: score only the structural planning artifacts the runner extracts, explicit scope, file-backed tasks, sequencing, and acceptance coverage. Do not require exact marker tags or one exact heading phrase when the same structure is otherwise observable. Do require task-level file ownership. For acceptance coverage, prefer task-level acceptance or success criteria, but match the current runner behavior when explicit acceptance structure is otherwise observable."
  }
}
