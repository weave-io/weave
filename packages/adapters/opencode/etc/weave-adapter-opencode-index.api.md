## API Report File for "@weaveio/weave-adapter-opencode"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Agent } from '@opencode-ai/sdk';
import { AgentConfig } from '@opencode-ai/sdk';
import { OpencodeClient } from '@opencode-ai/sdk';
import { Plugin as Plugin_2 } from '@opencode-ai/plugin';
import { PluginInput } from '@opencode-ai/plugin';
import { PluginModule } from '@opencode-ai/plugin';
import { Result } from 'neverthrow';
import { ResultAsync } from 'neverthrow';
import { z } from 'zod';

// @public
export interface AbortExecutionProjectionInput {
    readonly leaseId: string;
    readonly signal: "cancel" | "pause";
    // Warning: (ae-forgotten-export) The symbol "RuntimeStore" needs to be exported by the entry point index.d.ts
    readonly store: RuntimeStore;
    readonly workflowInstanceId: string;
}

// @public
export interface AdvanceStepProjectionInput {
    // Warning: (ae-forgotten-export) The symbol "AdvanceStepInput" needs to be exported by the entry point index.d.ts
    readonly completionSignal: AdvanceStepInput["completionSignal"];
    readonly context?: AdvanceStepInput["context"];
    readonly leaseId: string;
    // Warning: (ae-forgotten-export) The symbol "PlanStateProvider" needs to be exported by the entry point index.d.ts
    readonly planStateProvider?: PlanStateProvider;
    readonly stepName: string;
    readonly store: RuntimeStore;
    readonly workflowInstanceId: string;
}

// Warning: (ae-forgotten-export) The symbol "AdapterHealthReport" needs to be exported by the entry point index.d.ts
//
// @public
export function buildOpenCodeHealthReport(overrides?: {
    readonly commandEntrypointsReadiness?: "native" | "emulated" | "degraded" | "unsupported";
    readonly degradedOperations?: readonly string[];
    readonly unsupportedOperations?: readonly string[];
}): AdapterHealthReport;

// Warning: (ae-forgotten-export) The symbol "SkillInfo" needs to be exported by the entry point index.d.ts
//
// @public
export function buildSkillInfoList(names: string[]): SkillInfo[];

// @public
export function classifyExistingAgent(agentName: string, existingAgents: Agent[]): ReconcileDecision;

// Warning: (ae-forgotten-export) The symbol "InMemoryRuntimeStoreOptions" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "InMemoryRuntimeStore" needs to be exported by the entry point index.d.ts
//
// @public
export function createDefaultStore(options?: InMemoryRuntimeStoreOptions): InMemoryRuntimeStore;

// @public
export function createWeavePlugin(options?: WeavePluginOptions): Plugin_2;

// @public
export const DEFAULT_EXECUTION_WORKFLOW: "tapestry-execution";

// @public
export const DEFAULT_PLUGIN_LOG_SUBPATH = ".weave/weave.log";

// @public
export const DEGRADED_AFFORDANCES: readonly {
    readonly command: string;
    readonly reason: string;
    readonly equivalent: string;
}[];

// @public
export interface InspectStatusProjectionInput {
    readonly store: RuntimeStore;
    readonly workflowInstanceId: string;
}

// @public
export type ModelResolutionError = {
    type: "ModelNotAvailableError";
    agentName: string;
    requestedModels: string[];
    availableModels: string[];
    message: string;
} | {
    type: "ModelResolutionInputError";
    agentName: string;
    message: string;
};

// Warning: (ae-forgotten-export) The symbol "HarnessAdapter" needs to be exported by the entry point index.d.ts
//
// @public
export class OpenCodeAdapter implements HarnessAdapter {
    constructor(options?: OpenCodeAdapterOptions);
    init(): Promise<void>;
    loadAvailableSkills(): Promise<SkillInfo[]>;
    planStateProvider: PlanStateProvider | undefined;
    // Warning: (ae-forgotten-export) The symbol "AgentDescriptor" needs to be exported by the entry point index.d.ts
    spawnSubagent(descriptor: AgentDescriptor): ResultAsync<void, OpenCodeAdapterError>;
    readonly translatedAgents: Map<string, AgentConfig>;
}

// @public (undocumented)
export class OpenCodeAdapterError extends Error {
    constructor(input: {
        type: OpenCodeAdapterErrorType;
        agentName: string;
        message: string;
        cause?: unknown;
    });
    // (undocumented)
    readonly agentName: string;
    // (undocumented)
    readonly cause: unknown;
    // Warning: (ae-forgotten-export) The symbol "OpenCodeAdapterErrorType" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly type: OpenCodeAdapterErrorType;
}

// @public
export interface OpenCodeAdapterOptions {
    readonly availableSkills?: SkillInfo[];
    readonly client?: OpenCodeClientFacade;
    readonly modelContext?: OpenCodeModelContext;
    readonly projectRoot?: string;
}

// @public
export type OpenCodeClientError = {
    type: "ListAgentsError";
    message: string;
    cause?: unknown;
} | {
    type: "CreateAgentError";
    agentName: string;
    message: string;
    cause?: unknown;
} | {
    type: "UpdateAgentError";
    agentName: string;
    message: string;
    cause?: unknown;
};

// @public
export interface OpenCodeClientFacade {
    createAgent(name: string, config: AgentConfig): ResultAsync<void, OpenCodeClientError>;
    listAgents(): ResultAsync<Agent[], OpenCodeClientError>;
    updateAgent(name: string, config: AgentConfig): ResultAsync<void, OpenCodeClientError>;
}

// @public
export interface OpenCodeModelContext {
    availableModels?: Set<string>;
    systemDefault?: string;
    uiSelectedModel?: string;
}

export { Plugin_2 as Plugin }

export { PluginInput }

export { PluginModule }

// @public
export interface ProjectionDegraded<T> {
    // (undocumented)
    readonly command: string;
    // (undocumented)
    readonly data?: T;
    readonly hint?: string;
    readonly message: string;
    // (undocumented)
    readonly outcome: "degraded";
}

// @public
export interface ProjectionFailure {
    // (undocumented)
    readonly command: string;
    // Warning: (ae-forgotten-export) The symbol "CommandOperationError" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly error: CommandOperationError;
    readonly message: string;
    // (undocumented)
    readonly outcome: "failure";
}

// @public
export type ProjectionResult<T> = ProjectionSuccess<T> | ProjectionFailure | ProjectionDegraded<T>;

// @public
export interface ProjectionSuccess<T> {
    // (undocumented)
    readonly command: string;
    // (undocumented)
    readonly data: T;
    readonly message: string;
    // (undocumented)
    readonly outcome: "success";
}

// @public
export function reconcileAgent(agentName: string, config: AgentConfig, client: OpenCodeClientFacade): ResultAsync<void, ReconcileAgentError>;

// @public
export type ReconcileAgentError = {
    type: "ListAgentsError";
    message: string;
    cause?: unknown;
} | {
    type: "CreateAgentError";
    agentName: string;
    message: string;
    cause?: unknown;
} | {
    type: "UpdateAgentError";
    agentName: string;
    message: string;
    cause?: unknown;
} | {
    type: "CollisionError";
    agentName: string;
    message: string;
};

// @public
export type ReconcileDecision = "create" | "update" | "collision";

// @public
export function resolveModelForAgent(descriptor: AgentDescriptor, context: OpenCodeModelContext): Result<string, ModelResolutionError>;

// @public
export class RuntimeCommandProjection {
    // Warning: (ae-forgotten-export) The symbol "ExecutionAbortedData" needs to be exported by the entry point index.d.ts
    handleAbortExecution(input: AbortExecutionProjectionInput): Promise<ProjectionResult<ExecutionAbortedData>>;
    // Warning: (ae-forgotten-export) The symbol "StepAdvancedData" needs to be exported by the entry point index.d.ts
    handleAdvanceStep(input: AdvanceStepProjectionInput): Promise<ProjectionResult<StepAdvancedData>>;
    // Warning: (ae-forgotten-export) The symbol "ExecutionStatusData" needs to be exported by the entry point index.d.ts
    handleInspectStatus(input: InspectStatusProjectionInput): Promise<ProjectionResult<ExecutionStatusData>>;
    // Warning: (ae-forgotten-export) The symbol "RuntimeHealthData" needs to be exported by the entry point index.d.ts
    handleRuntimeHealth(input: RuntimeHealthProjectionInput): Promise<ProjectionResult<RuntimeHealthData>>;
    handleRunWorkflow(input: RunWorkflowProjectionInput): Promise<ProjectionResult<ExecutionStartedData>>;
    // Warning: (ae-forgotten-export) The symbol "ExecutionStartedData" needs to be exported by the entry point index.d.ts
    handleStartPlan(input: StartPlanProjectionInput): Promise<ProjectionResult<ExecutionStartedData>>;
}

// @public
export interface RuntimeHealthProjectionInput {
    readonly degradedOperations?: readonly string[];
    readonly healthReport: AdapterHealthReport;
    readonly unsupportedOperations?: readonly string[];
}

// @public
export function runWorkflow(input: RunWorkflowInput): ResultAsync<RunWorkflowResult, RunWorkflowError>;

// @public
export type RunWorkflowError = {
    readonly type: "LifecycleError";
    readonly cause: LifecycleError;
} | {
    readonly type: "WorkflowNotFound";
    readonly workflowName: string;
} | {
    readonly type: "MaxStepsExceeded";
    readonly maxSteps: number;
};

// @public
export interface RunWorkflowInput {
    readonly adapter: OpenCodeAdapter;
    // Warning: (ae-forgotten-export) The symbol "WeaveConfig" needs to be exported by the entry point index.d.ts
    readonly config: WeaveConfig;
    readonly goal: string;
    readonly maxSteps?: number;
    readonly ownerId?: string;
    readonly planStateProvider?: PlanStateProvider;
    readonly slug: string;
    readonly store?: RuntimeStore;
    readonly workflowName: string;
}

// @public
export interface RunWorkflowProjectionInput {
    readonly adapter: OpenCodeAdapter;
    readonly goal: string;
    readonly now?: string;
    readonly ownerId: string;
    readonly planStateProvider?: PlanStateProvider;
    readonly slug: string;
    readonly store: RuntimeStore;
    readonly workflowName: string;
    readonly workflows: Record<string, unknown>;
}

// @public
export interface RunWorkflowResult {
    // Warning: (ae-forgotten-export) The symbol "LifecycleEffect" needs to be exported by the entry point index.d.ts
    readonly appliedEffects: readonly LifecycleEffect[];
    readonly status: "completed" | "paused";
    readonly stepsDispatched: number;
    readonly workflowInstanceId: string;
}

// @public
export class SdkOpenCodeClient implements OpenCodeClientFacade {
    constructor(client: OpencodeClient);
    // (undocumented)
    createAgent(name: string, config: AgentConfig): ResultAsync<void, OpenCodeClientError>;
    // (undocumented)
    listAgents(): ResultAsync<Agent[], OpenCodeClientError>;
    // (undocumented)
    updateAgent(name: string, config: AgentConfig): ResultAsync<void, OpenCodeClientError>;
}

// @public
export const START_WORK_COMMAND_TEMPLATE: string;

// @public
export function startPlanExecution(input: StartPlanExecutionInput): ResultAsync<RunWorkflowResult, StartPlanExecutionError>;

// @public
export type StartPlanExecutionError = {
    readonly type: "PlanNotFound";
    readonly planName: string;
} | {
    readonly type: "InvalidPlanName";
    readonly planName: string;
} | {
    readonly type: "ProviderUnavailable";
    readonly cause: Error | {
        readonly message: string;
    };
} | {
    readonly type: "WorkflowError";
    readonly cause: RunWorkflowError;
};

// @public
export interface StartPlanExecutionInput {
    readonly adapter: OpenCodeAdapter;
    readonly config: RunWorkflowInput["config"];
    readonly goal?: string;
    readonly planName: string;
    readonly planStateProvider: PlanStateProvider | undefined;
    readonly store?: RuntimeStore;
    readonly workflowName?: string;
}

// @public
export interface StartPlanProjectionInput {
    readonly adapter: OpenCodeAdapter;
    readonly goal: string;
    readonly now?: string;
    readonly ownerId: string;
    readonly planName: string;
    readonly planStateProvider: PlanStateProvider;
    readonly slug: string;
    readonly store: RuntimeStore;
    readonly workflowName: string;
    readonly workflows: Record<string, unknown>;
}

// @public
export function tagWithOwnership(config: AgentConfig): AgentConfig;

// Warning: (ae-forgotten-export) The symbol "MissingSkillsError" needs to be exported by the entry point index.d.ts
//
// @public
export function validateDeclaredSkills(declaredSkills: string[], availableSkills: SkillInfo[], disabledSkills?: string[]): Result<void, MissingSkillsError>;

// @public
export const WEAVE_COMMAND_LABELS: {
    readonly startPlan: "/weave:start";
    readonly runWorkflow: "/weave:run";
    readonly status: "/weave:status";
    readonly abort: "/weave:abort";
    readonly advance: "/weave:advance";
    readonly health: "/weave:health";
};

// @public
export const WEAVE_OWNERSHIP_TAG = "[weave-managed]";

// @public
export const WEAVE_START_COMMAND: "/weave:start";

// @public
export const WEAVE_START_COMMAND_TEMPLATE: string;

// @public
export const WEAVE_START_LEGACY_COMMAND: "/start-work";

// @public
const WeavePlugin: Plugin_2;
export { WeavePlugin }
export default WeavePlugin;

// @public
export interface WeavePluginOptions {
    readonly clientFacade?: OpenCodeClientFacade;
    // Warning: (ae-forgotten-export) The symbol "FileReader_2" needs to be exported by the entry point index.d.ts
    readonly fileReader?: FileReader_2;
}

// @public
export const WeavePluginServer: Plugin_2;

// Warnings were encountered during analysis:
//
// dist-types/run-workflow.d.ts:47:5 - (ae-forgotten-export) The symbol "LifecycleError" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
