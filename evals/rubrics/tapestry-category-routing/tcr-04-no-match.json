{
  "case_id": "tcr-04-no-match",
  "suite": "tapestry-category-routing",
  "scoring": {
    "outcome_weight": 0.6,
    "per_expectation_weight": 0.4,
    "required": true,
    "notes": "Required case. Pass gate: routingCorrectness >= 0.95 (deterministic) AND, because transcript_expectations is non-empty and a scorer is present, average qualitative score (delegationCorrectness + executionCompleteness + rationaleQuality) / 3 >= 0.7. `src/Infrastructure/Logging/Logger.cs` does not match the only declared category pattern `src/4.Presentation/DST.Client/**`. The model must affirmatively route to the generic `shuttle` agent by name — the runner's deterministic extraction (`analyzeCategoryRouting`) only recognizes `shuttle` or `shuttle-{category}` tokens, so a response that mentions `loom` without an affirmative `shuttle` routing line is an `extraction-miss` and scores 0.0 on routingCorrectness, not partial credit. `loom` is intentionally NOT an accepted_alternate here — it can never receive deterministic routing credit. Do not credit `shuttle-client-frontend` — routing an infra file to the client-frontend shuttle is a clear mismatch. The rationale should explain that no category pattern matched and name `shuttle` as the fallback."
  }
}
