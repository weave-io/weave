{
  "case_id": "tcr-09-similar-names",
  "suite": "tapestry-category-routing",
  "scoring": {
    "outcome_weight": 0.75,
    "per_expectation_weight": 0.25,
    "required": true,
    "notes": "Fairness/alignment: `src/4.Presentation/DST.Client/Components/Header.tsx` matches `src/4.Presentation/DST.Client/**` (client-frontend) and does NOT match `src/4.Presentation/DST.Mobile/**` (client-mobile). The model must distinguish `DST.Client` from `DST.Mobile` — these are different path segments. Score `shuttle-client-frontend` as exact-category-match (1.0). Score `shuttle-client-mobile` as wrong-category (0.0). The rationale must cite the path segment `DST.Client` vs `DST.Mobile` as the distinguishing evidence."
  }
}
