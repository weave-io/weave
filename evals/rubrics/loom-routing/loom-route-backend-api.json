{
  "case_id": "loom-route-backend-api",
  "suite": "loom-routing",
  "scoring": {
    "outcome_weight": 0.8,
    "per_expectation_weight": 0.2,
    "required": true,
    "notes": "Routing to the generic shuttle is the primary signal in the current text-only VNext fixture set; thread may appear as evidence gathering before implementation, and reviewer/security follow-ups should not invalidate the primary route."
  }
}
