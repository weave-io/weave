---
title: Install And Build Moved
description: Compatibility route for setup and operations docs.
---

This page is kept for existing links after the public docs consolidation. First setup lives in [Getting Started](../../getting-started/), while build, typecheck, test, and docs commands live in [Runtime Operations](../../runtime-operations/).

Current support note: use `bun run build`, `bun run typecheck`, and `bun run docs:build`; there is no current `weave build` command.
