{
  "case_id": "tcr-01-exact-match",
  "suite": "tapestry-category-routing",
  "scoring": {
    "outcome_weight": 0.7,
    "per_expectation_weight": 0.3,
    "required": true,
    "notes": "Fairness/alignment: styling `src/4.Presentation/DST.Client/Components/Settings.tsx` is an unambiguous match for the `client-frontend` category description and its trigger `Use for DST client web UI work`. Score `shuttle-client-frontend` as exact-category-match (1.0). Penalise generic `shuttle` or `loom` routing — these are fallbacks that should not be used when the category domain is a clear match. The rationale must cite the file, the category description, or the trigger as evidence."
  }
}
