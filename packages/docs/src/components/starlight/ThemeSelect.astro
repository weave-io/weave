---
/**
 * ThemeSelect override — intentionally empty.
 *
 * Starlight's default theme picker (`<starlight-theme-select>`) is replaced by
 * the prototype `.theme-toggle` control rendered in the PageFrame topbar, which
 * is driven by theme.js against the `weave-theme` contract. Rendering nothing
 * removes Starlight's select-based theme switcher and its styling.
 */
---
