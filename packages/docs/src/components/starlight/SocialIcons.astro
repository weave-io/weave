---
/**
 * SocialIcons override — intentionally empty.
 *
 * The prototype docs topbar carries its own GitHub link (PageFrame override),
 * so Starlight's default social-icon row (rendered in the default Header) is
 * not needed. Rendering nothing removes the default social icons and the
 * trailing divider styling from the (already-empty) header path.
 */
---
