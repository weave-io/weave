{
  "case_id": "loom-route-backend-api",
  "suite": "loom-routing",
  "scoring": {
    "outcome_weight": 0.8,
    "per_expectation_weight": 0.2,
    "required": true,
    "notes": "Fairness/alignment: score only observable text routing. Credit the primary implementation route to `shuttle`, treat category shuttles as equivalent to `shuttle`, ignore exploratory `thread` context and follow-up reviewer or security mentions when they are not the primary implementation target, and do not require real delegation telemetry."
  }
}
