# Trajectory eval fixture: builtin agents only.
#
# Sub-agent models are pinned to the model under test by the runner's
# generated global config (Spec 35), so none are declared here.
settings {
  log_level INFO
}
