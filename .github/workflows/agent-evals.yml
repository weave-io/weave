name: Agent Evals

# Manual-only: no push/PR/schedule triggers.
# Only a repository collaborator with workflow_dispatch permission can run this.
on:
  workflow_dispatch:
    inputs:
      agent:
        description: |
          Optional agent name filter. Leave blank to run all eight text-only suite families.
          Allowed values are validated against the shared eval suite registry:
          loom, tapestry, shuttle, spindle, pattern, weft, warp, loom-routing, tapestry-execution, tapestry-category-routing, shuttle-execution, spindle-tools, pattern-planning, weft-review, warp-security
        required: false
        default: ""
        type: string
      model:
        description: |
          Optional model ID filter. Leave blank to run the model set chosen below.
          Allowed values are every `id` in evals/model-matrix.json; an unknown
          value is rejected before any eval runs.
        required: false
        default: ""
        type: string
      models:
        description: |
          Model set to run when no model ID is given. `default` runs the full
          default matrix; `dev` runs only the cheap development subset (the
          `dev: true` entries in evals/model-matrix.json). `dev` cannot be
          combined with a model ID.
        required: false
        default: "default"
        type: choice
        options:
          - default
          - dev
      case:
        description: |
          Optional eval case ID filter. Leave blank to run all cases.
          Allowed values (hardcoded from evals/cases/**):
            loom-route-backend-api
            loom-route-frontend-ui
            loom-route-ambiguous-direct-shuttle
            loom-route-shuttle-implement-utility
            loom-route-shuttle-implement-utility-trajectory
            loom-route-shuttle-boundary-plan-first
            loom-route-pattern-plan-migration
            loom-route-pattern-boundary-small-fix
            loom-route-thread-explore-auth-flow
            loom-route-thread-boundary-prehop-then-implement
            loom-route-spindle-research-oauth-pkce
            loom-route-spindle-boundary-internal-exploration
            loom-route-weft-review-checkout-pr
            loom-route-weft-boundary-downstream-review
            loom-route-warp-security-audit-auth-flow
            loom-route-warp-boundary-downstream-audit
            loom-delegates-backend-fix-to-category-trajectory
            loom-runs-the-failing-check-itself-trajectory
            tapestry-execute-plan-step
            tapestry-delegate-to-shuttle
            tapestry-rejects-contradicted-report
            tapestry-accepts-evidenced-report
            tapestry-runs-plan-verification-trajectory
            tapestry-dispatches-independent-tasks-in-parallel-trajectory
            tcr-01-exact-match
            tcr-02-multiple-files
            tcr-03-windows-paths
            tcr-04-no-match
            tcr-05-cross-category
            tcr-06-overlap
            tcr-07-explicit-hint
            tcr-08-misleading-prose
            tcr-09-similar-names
            tcr-10-disabled-category
            shuttle-execution-report-structured-evidence
            shuttle-execution-report-tests-and-assumptions
            shuttle-execution-reports-unverified
            shuttle-verify-tests-after-edit-trajectory
            spindle-tools-citations-facts-confidence
            spindle-tools-source-boundary-network-claims
            pattern-plan-settings-refactor
            pattern-plan-release-checklist
            pattern-plan-verify-by-per-criterion
            pattern-plan-no-invented-commands
            weft-review-clean-approval
            weft-review-reject-blocker-citation
            weft-review-traced-true-positive
            weft-review-guarded-false-positive
            warp-security-fast-exit-approve
            warp-security-block-evidence-findings
            warp-security-traced-injection
            warp-security-guarded-false-positive
          A regex-only guard is applied as a second defence for forward-compatibility.
          The workflow dry-run step validates this same allowlist before any live model call.
        required: false
        default: ""
        type: string
      repeat:
        description: |
          How many times each selected case runs per model (`weave eval run --repeat`).
          A whole number from 1 to 20; blank runs each case once. Applies to both the
          text-only and the trajectory job.
        required: false
        default: ""
        type: string
      trajectory:
        description: |
          Also run the harness trajectory job (every `harness_trajectory` case the
          other filters select, in a Podman sandbox). On by default; untick to run
          the text-only job alone.
        required: false
        default: true
        type: boolean

# Minimal permissions: read-only for the source repo.
#
# PUBLISH POLICY:
#   External publication of eval results to weave-io/weave-agent-evals is
#   handled by the CLI's GitHubContentsPublisher via WEAVE_EVAL_PUBLISH_MODE=publish
#   and EVAL_RESULTS_REPO_TOKEN. The token is consumed only by the CLI and passed
#   exclusively as an HTTP Authorization header — it never appears in shell command
#   arguments, workflow logs, or artifact content.
#
#   The CLI's ArtifactBundleWriter runs the full sanitizer pipeline before any
#   external push:
#     1. assertPublishSafe() on every assembled object
#     2. assertJsonPublishSafe() on every serialized JSON string
#     3. enforcePublishPolicy() (dry-run blocking, score file presence check)
#
#   Raw artifacts (raw/ subdirectory) are NEVER published. The CLI's
#   GitHubContentsPublisher discovers only top-level *.json files and
#   explicitly excludes anything under raw/.
#
#   Do NOT add manual git clone/push with EVAL_RESULTS_REPO_TOKEN in the URL —
#   that would bypass the CLI's sanitizer and expose the token in process args.
#
# id-token: none   — no OIDC needed (no cloud provider push)
# packages: none   — no package registry operations
# pull-requests: none — no PR write operations
permissions: {}

jobs:
  # ---------------------------------------------------------------------------
  # Job 1: Validate dispatch inputs before any eval execution.
  #
  # Raw dispatch inputs are validated against strict allowlists and regex
  # BEFORE reaching the eval job. Validation failures abort the workflow
  # without any eval execution or secret access.
  # ---------------------------------------------------------------------------
  validate-inputs:
    name: Validate dispatch inputs
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      agent: ${{ steps.check.outputs.agent }}
      model: ${{ steps.check.outputs.model }}
      models: ${{ steps.check.outputs.models }}
      case:  ${{ steps.check.outputs.case }}
      repeat: ${{ steps.check.outputs.repeat }}
      run_text: ${{ steps.check.outputs.run_text }}
      run_trajectory: ${{ steps.check.outputs.run_trajectory }}
    steps:
      # The model allowlist is derived from evals/model-matrix.json rather than
      # restated here, so adding a model is a single edit to that file. Reading
      # it needs the repo, hence this checkout. No secrets are available to this
      # job, and eval execution still happens only after validation passes.
      - name: Checkout source repo
        # actions/checkout v4.2.2
        # SHA pinned to the upstream commit for this tag — immune to tag mutation.
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false

      - name: Validate and sanitize inputs
        id: check
        # Raw dispatch inputs are bound to env vars before any processing.
        # They are NEVER interpolated into shell strings (${{ ... }} expands
        # only in the `env:` block, not in command arguments).
        env:
          RAW_AGENT: ${{ github.event.inputs.agent }}
          RAW_MODEL: ${{ github.event.inputs.model }}
          RAW_MODELS: ${{ github.event.inputs.models }}
          RAW_CASE:  ${{ github.event.inputs.case }}
          RAW_REPEAT: ${{ github.event.inputs.repeat }}
          RAW_TRAJECTORY: ${{ github.event.inputs.trajectory }}
        run: |
          set -euo pipefail

          # ---------------------------------------------------------------------------
          # Allowlists — kept in sync with model-matrix.json and evals/cases/**/
          # ---------------------------------------------------------------------------

          # Agent allowlist: suite names and logical agent names.
          # Short agent names and suite IDs are both accepted to mirror
          # EvalOrchestrator.shouldRunSuite() which matches on either.
          ALLOWED_AGENTS="loom tapestry shuttle spindle pattern weft warp loom-routing tapestry-execution tapestry-category-routing shuttle-execution spindle-tools pattern-planning weft-review warp-security"

          # Model allowlist: read from evals/model-matrix.json, which is the
          # single source of truth. Adding a model there reaches this workflow,
          # the CLI and every case fixture without another edit.
          ALLOWED_MODELS="$(jq -r '.models[].id' evals/model-matrix.json | tr '\n' ' ')"
          if [ -z "${ALLOWED_MODELS// /}" ]; then
            echo "::error::Could not read model IDs from evals/model-matrix.json"
            exit 1
          fi

          # Model set allowlist: the named sets `--models` accepts. Mirrors
          # MODEL_SET_NAMES in packages/cli/src/evals/model-matrix.ts;
          # workflow-sync.test.ts enforces parity. The models in each set are
          # read from evals/model-matrix.json by the CLI, never listed here.
          ALLOWED_MODEL_SETS="default dev"

          # Case allowlist: exact case IDs from evals/cases/**/
          # This workflow must stay in sync with the shared registry-backed CLI
          # validation and fixture set; workflow-sync.test.ts enforces parity.
          ALLOWED_CASES="loom-route-backend-api loom-route-frontend-ui loom-route-ambiguous-direct-shuttle loom-route-shuttle-implement-utility loom-route-shuttle-implement-utility-trajectory loom-route-shuttle-boundary-plan-first loom-route-pattern-plan-migration loom-route-pattern-boundary-small-fix loom-route-thread-explore-auth-flow loom-route-thread-boundary-prehop-then-implement loom-route-spindle-research-oauth-pkce loom-route-spindle-boundary-internal-exploration loom-route-weft-review-checkout-pr loom-route-weft-boundary-downstream-review loom-route-warp-security-audit-auth-flow loom-route-warp-boundary-downstream-audit loom-delegates-backend-fix-to-category-trajectory loom-runs-the-failing-check-itself-trajectory tapestry-execute-plan-step tapestry-delegate-to-shuttle tapestry-rejects-contradicted-report tapestry-accepts-evidenced-report tapestry-runs-plan-verification-trajectory tapestry-dispatches-independent-tasks-in-parallel-trajectory shuttle-execution-report-structured-evidence shuttle-execution-report-tests-and-assumptions shuttle-execution-reports-unverified shuttle-verify-tests-after-edit-trajectory spindle-tools-citations-facts-confidence spindle-tools-source-boundary-network-claims pattern-plan-settings-refactor pattern-plan-release-checklist pattern-plan-verify-by-per-criterion pattern-plan-no-invented-commands weft-review-clean-approval weft-review-reject-blocker-citation weft-review-traced-true-positive weft-review-guarded-false-positive warp-security-fast-exit-approve warp-security-block-evidence-findings warp-security-traced-injection warp-security-guarded-false-positive tcr-01-exact-match tcr-02-multiple-files tcr-03-windows-paths tcr-04-no-match tcr-05-cross-category tcr-06-overlap tcr-07-explicit-hint tcr-08-misleading-prose tcr-09-similar-names tcr-10-disabled-category"

          # Identifier regex: mirrors VALID_IDENTIFIER_RE from input-validation.ts.
          # Anchored; only alphanumerics, underscore, dot, slash, colon, at, hyphen.
          # This is a second-layer guard for forward-compatibility.
          IDENT_RE='^[A-Za-z0-9_./:@-]+$'

          # ---------------------------------------------------------------------------
          # Validate --agent input
          # ---------------------------------------------------------------------------
          AGENT_OUT=""
          if [ -n "${RAW_AGENT}" ]; then
            if ! printf '%s' "${RAW_AGENT}" | grep -qE "${IDENT_RE}"; then
              echo "::error::Invalid agent input: only A-Za-z0-9_./:@- are allowed"
              exit 1
            fi
            MATCHED=false
            for a in ${ALLOWED_AGENTS}; do
              if [ "${RAW_AGENT}" = "${a}" ]; then MATCHED=true; break; fi
            done
            if [ "${MATCHED}" = "false" ]; then
              echo "::error::Unknown agent '${RAW_AGENT}'. Allowed: ${ALLOWED_AGENTS}"
              exit 1
            fi
            AGENT_OUT="${RAW_AGENT}"
          fi

          # ---------------------------------------------------------------------------
          # Validate --model input
          # ---------------------------------------------------------------------------
          MODEL_OUT=""
          if [ -n "${RAW_MODEL}" ]; then
            if ! printf '%s' "${RAW_MODEL}" | grep -qE "${IDENT_RE}"; then
              echo "::error::Invalid model input: only A-Za-z0-9_./:@- are allowed"
              exit 1
            fi
            MATCHED=false
            for m in ${ALLOWED_MODELS}; do
              if [ "${RAW_MODEL}" = "${m}" ]; then MATCHED=true; break; fi
            done
            if [ "${MATCHED}" = "false" ]; then
              echo "::error::Unknown model '${RAW_MODEL}'. Allowed: ${ALLOWED_MODELS}"
              exit 1
            fi
            MODEL_OUT="${RAW_MODEL}"
          fi

          # ---------------------------------------------------------------------------
          # Validate --models input (a choice input, validated again here)
          # ---------------------------------------------------------------------------
          MODELS_OUT=""
          if [ -n "${RAW_MODELS}" ]; then
            MATCHED=false
            for s in ${ALLOWED_MODEL_SETS}; do
              if [ "${RAW_MODELS}" = "${s}" ]; then MATCHED=true; break; fi
            done
            if [ "${MATCHED}" = "false" ]; then
              echo "::error::Unknown model set '${RAW_MODELS}'. Allowed: ${ALLOWED_MODEL_SETS}"
              exit 1
            fi
            MODELS_OUT="${RAW_MODELS}"
          fi
          if [ "${MODELS_OUT}" = "dev" ] && [ -n "${MODEL_OUT}" ]; then
            echo "::error::The dev model set cannot be combined with a model ID; clear one of them"
            exit 1
          fi

          # ---------------------------------------------------------------------------
          # Validate --case input
          #
          # Validated against the hardcoded case allowlist from evals/cases/**
          # (see the ALLOWED_CASES variable above). The regex guard is applied
          # first as a fast-fail for obviously malformed values, then the allowlist
          # enforces only known case IDs can be passed to the CLI.
          #
          # The allowlist MUST be kept in sync with evals/cases/**. When a new case
          # is added under evals/cases/, add its "id" field value to ALLOWED_CASES
          # and to the input description at the top of this file.
          # ---------------------------------------------------------------------------
          CASE_OUT=""
          if [ -n "${RAW_CASE}" ]; then
            if ! printf '%s' "${RAW_CASE}" | grep -qE "${IDENT_RE}"; then
              echo "::error::Invalid case input: only A-Za-z0-9_./:@- are allowed"
              exit 1
            fi
            MATCHED=false
            for c in ${ALLOWED_CASES}; do
              if [ "${RAW_CASE}" = "${c}" ]; then MATCHED=true; break; fi
            done
            if [ "${MATCHED}" = "false" ]; then
              echo "::error::Unknown case '${RAW_CASE}'. Allowed: ${ALLOWED_CASES}"
              exit 1
            fi
            CASE_OUT="${RAW_CASE}"
          fi

          # ---------------------------------------------------------------------------
          # Validate --repeat input
          #
          # Blank means each case runs once. Otherwise a plain whole number from
          # 1 to MAX_REPEAT, which mirrors MAX_EVAL_REPEAT in
          # packages/cli/src/evals/input-validation.ts (workflow-sync.test.ts
          # enforces parity). The CLI validates the value again.
          # ---------------------------------------------------------------------------
          MAX_REPEAT=20
          REPEAT_OUT=""
          if [ -n "${RAW_REPEAT}" ]; then
            if ! printf '%s' "${RAW_REPEAT}" | grep -qE '^[1-9][0-9]*$'; then
              echo "::error::Invalid repeat input: a whole number from 1 to ${MAX_REPEAT} is required"
              exit 1
            fi
            if [ "${#RAW_REPEAT}" -gt 2 ] || [ "${RAW_REPEAT}" -gt "${MAX_REPEAT}" ]; then
              echo "::error::Invalid repeat input: at most ${MAX_REPEAT} repeats"
              exit 1
            fi
            REPEAT_OUT="${RAW_REPEAT}"
          fi

          # ---------------------------------------------------------------------------
          # Validate --trajectory input (a boolean input, validated again here)
          # ---------------------------------------------------------------------------
          case "${RAW_TRAJECTORY}" in
            true|"") TRAJECTORY_ON=true ;;
            false) TRAJECTORY_ON=false ;;
            *)
              echo "::error::Invalid trajectory input: true or false is required"
              exit 1
              ;;
          esac

          # ---------------------------------------------------------------------------
          # Decide which eval jobs have work.
          #
          # The text-only job runs `weave eval run --track text`, the trajectory
          # job `--track trajectory`. A job whose track the filters exclude is
          # skipped rather than failed:
          #   - a case filter runs only the job of that case's track;
          #   - the text job needs a model filter that some text-only case
          #     allows (ALLOWED_TEXT_MODELS: the default and dev sets, which
          #     cases without `allowed_models` inherit, plus any model a text
          #     case names itself);
          #   - the trajectory job needs an agent filter that names a suite
          #     holding trajectory cases, and a model filter that some
          #     trajectory case allows.
          #
          # ALLOWED_TRAJECTORY_CASES: exact IDs of the harness_trajectory cases
          # under evals/cases/**. ALLOWED_TRAJECTORY_AGENTS: the suite IDs and
          # short agent names of the suites that can hold them (the registry
          # entries allowing harness_trajectory). workflow-sync.test.ts enforces
          # both. ALLOWED_TRAJECTORY_MODELS is the union of those cases'
          # allowed_models, read from the fixtures rather than restated.
          # ---------------------------------------------------------------------------
          ALLOWED_TRAJECTORY_CASES="loom-route-shuttle-implement-utility-trajectory loom-delegates-backend-fix-to-category-trajectory loom-runs-the-failing-check-itself-trajectory shuttle-verify-tests-after-edit-trajectory tapestry-runs-plan-verification-trajectory tapestry-dispatches-independent-tasks-in-parallel-trajectory"
          ALLOWED_TRAJECTORY_AGENTS="loom tapestry shuttle loom-routing tapestry-execution shuttle-execution"
          ALLOWED_TRAJECTORY_MODELS="$(jq -rs '[.[] | select(.expected_outcome.kind=="harness_trajectory") | .allowed_models[]?] | unique | .[]' evals/cases/*/*.json | tr '\n' ' ')"
          if [ -z "${ALLOWED_TRAJECTORY_MODELS// /}" ]; then
            echo "::error::Could not derive trajectory model allowlist from evals/cases"
            exit 1
          fi

          ALLOWED_TEXT_MODELS="$( {
            jq -r '.models[] | select(.default == true or .dev == true) | .id' evals/model-matrix.json
            jq -rs '[.[] | select(.expected_outcome.kind != "harness_trajectory") | .allowed_models[]?] | unique | .[]' evals/cases/*/*.json
          } | sort -u | tr '\n' ' ')"

          in_list() {
            for item in $2; do
              if [ "$1" = "${item}" ]; then return 0; fi
            done
            return 1
          }

          RUN_TEXT=true
          RUN_TRAJECTORY="${TRAJECTORY_ON}"
          if [ -n "${CASE_OUT}" ]; then
            if in_list "${CASE_OUT}" "${ALLOWED_TRAJECTORY_CASES}"; then
              RUN_TEXT=false
            else
              RUN_TRAJECTORY=false
            fi
          fi
          if [ -n "${MODEL_OUT}" ] && ! in_list "${MODEL_OUT}" "${ALLOWED_TEXT_MODELS}"; then
            RUN_TEXT=false
          fi
          if [ -n "${AGENT_OUT}" ] && ! in_list "${AGENT_OUT}" "${ALLOWED_TRAJECTORY_AGENTS}"; then
            RUN_TRAJECTORY=false
          fi
          if [ -n "${MODEL_OUT}" ] && ! in_list "${MODEL_OUT}" "${ALLOWED_TRAJECTORY_MODELS}"; then
            RUN_TRAJECTORY=false
          fi

          # ---------------------------------------------------------------------------
          # Emit validated outputs — never the raw dispatch values.
          # Downstream jobs reference these outputs, not the raw inputs.
          # ---------------------------------------------------------------------------
          {
            printf 'agent=%s\n' "${AGENT_OUT}"
            printf 'model=%s\n' "${MODEL_OUT}"
            printf 'models=%s\n' "${MODELS_OUT}"
            printf 'case=%s\n'  "${CASE_OUT}"
            printf 'repeat=%s\n' "${REPEAT_OUT}"
            printf 'run_text=%s\n' "${RUN_TEXT}"
            printf 'run_trajectory=%s\n' "${RUN_TRAJECTORY}"
          } >> "${GITHUB_OUTPUT}"

          echo "Validation passed: agent='${AGENT_OUT}' model='${MODEL_OUT}' models='${MODELS_OUT}' case='${CASE_OUT}' repeat='${REPEAT_OUT}'"
          echo "Jobs: text=${RUN_TEXT} trajectory=${RUN_TRAJECTORY}"

  # ---------------------------------------------------------------------------
  # Job 2: Run evals and publish sanitized bundles to weave-io/weave-agent-evals.
  #
  # SECURITY CONTRACT:
  #   - Source checkout uses persist-credentials: false — no implicit
  #     GITHUB_TOKEN write capability in later steps.
  #   - OPENROUTER_API_KEY is scoped to the eval run step only; it is not
  #     present in any other step.
  #   - EVAL_RESULTS_REPO_TOKEN is scoped to the eval run step only; it is not
  #     present in any other step. The CLI reads this token and passes it
  #     exclusively as an HTTP Authorization header to the GitHub Contents API.
  #     The token never appears in shell command arguments, log output, or
  #     serialized artifact content.
  #   - WEAVE_EVAL_PUBLISH_MODE=publish activates the GitHubContentsPublisher
  #     in the CLI's EvalOrchestrator. Only sanitized bundle files (*.json at
  #     the top level of eval-bundles/<bundle-dir>/) are published; the raw/
  #     subdirectory is never included.
  #   - No git clone/push with tokens in shell command arguments or URL strings.
  #   - No manual bypass of the CLI's sanitizer/publish policy.
  #   - Upload-artifact is kept as a backup for local inspection; it reads only
  #     from the workspace path and uses no stored secrets.
  # ---------------------------------------------------------------------------
  run-evals:
    name: Run agent evals
    runs-on: ubuntu-latest
    needs: validate-inputs
    # Skipped when a case filter names a trajectory case, or a model filter
    # names a model no text-only case allows.
    if: needs.validate-inputs.outputs.run_text == 'true'
    permissions:
      contents: read
    steps:
      # -----------------------------------------------------------------------
      # Step 1: Checkout source repo.
      #
      # persist-credentials: false — the GITHUB_TOKEN credential is dropped
      # immediately after checkout. This ensures no implicit source-repo write
      # capability leaks into later steps.
      # -----------------------------------------------------------------------
      - name: Checkout source repo
        # actions/checkout v4.2.2
        # SHA pinned to the upstream commit for this tag — immune to tag mutation.
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false

      # -----------------------------------------------------------------------
      # Step 2: Set up Bun runtime.
      #
      # Pinned by full commit SHA to prevent supply-chain substitution.
      # -----------------------------------------------------------------------
      - name: Set up Bun
        # oven-sh/setup-bun v2.2.0
        # SHA pinned to the upstream commit for this tag.
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: "1.3.10"

      # -----------------------------------------------------------------------
      # Step 3: Install dependencies.
      #
      # --frozen-lockfile: any lockfile drift causes a hard failure here rather
      # than silently upgrading transitive dependencies at eval time.
      # -----------------------------------------------------------------------
      - name: Install dependencies
        run: bun install --frozen-lockfile

      # -----------------------------------------------------------------------
      # Step 4: Build the CLI.
      # -----------------------------------------------------------------------
      - name: Build
        run: bun run build

      # -----------------------------------------------------------------------
      # Step 5: Run agent evals and publish results.
      #
      # Filters are passed as WEAVE_EVAL_* environment variables, NOT as argv.
      # parseEvalRunRequest() in the CLI reads these env vars, validates them
      # again (regex + duplicate-conflict check), and merges them into the
      # trusted EvalRunRequest. The CLI command receives no filter arguments.
      #
      # OPENROUTER_API_KEY and EVAL_RESULTS_REPO_TOKEN are both scoped to
      # this step only — they are not available in any other step.
      #
      # SECURITY — token handling:
      #   - EVAL_RESULTS_REPO_TOKEN is read by GitHubContentsPublisher in the
      #     CLI and passed ONLY as an HTTP Authorization header to the GitHub
      #     Contents API. It is never included in shell command arguments,
      #     log messages, serialized JSON, or workflow artifacts.
      #   - WEAVE_EVAL_PUBLISH_MODE=publish enables GitHubContentsPublisher.
      #     The CLI's ArtifactBundleWriter runs assertPublishSafe() and
      #     assertJsonPublishSafe() on every artifact before publication.
      #   - Only top-level *.json files from eval-bundles/<bundle-dir>/ are
      #     published; the raw/ subdirectory is explicitly excluded by the CLI.
      #   - Do NOT add raw-artifact flags or bypass the CLI sanitizer.
      # -----------------------------------------------------------------------
      - name: Dry-run agent evals in CI env
        env:
          # No secrets are needed for dry-run. This step exercises the same
          # no-argv filter path as the publish step without model calls or writes.
          # It still validates the same registry-backed suite/model/case inputs
          # and fail-closes on invalid filters or text-only contract violations.
          # It is the documented contributor preflight path for validating
          # suite/model/case filters without OPENROUTER_API_KEY.
          WEAVE_EVAL_PUBLISH_MODE: "publish"
          WEAVE_EVAL_AGENT: ${{ needs.validate-inputs.outputs.agent }}
          WEAVE_EVAL_MODEL: ${{ needs.validate-inputs.outputs.model }}
          WEAVE_EVAL_MODELS: ${{ needs.validate-inputs.outputs.models }}
          WEAVE_EVAL_CASE:  ${{ needs.validate-inputs.outputs.case }}
          WEAVE_EVAL_REPEAT: ${{ needs.validate-inputs.outputs.repeat }}
          # Text-only cases only: harness_trajectory cases need the sandbox
          # image, which only the trajectory-evals job builds.
          WEAVE_EVAL_TRACK: "text"
          CI: "true"
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          set -euo pipefail
          # No filter flags in argv. This verifies that blank dispatch inputs
          # and eval control env vars are accepted by the CLI in GitHub Actions.
          bun packages/cli/src/main.ts eval run --dry-run

      - name: Run agent evals
        env:
          # Secret: OpenRouter inference API key.
          # Scoped to this step only — not available in any other step.
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          # Secret: GitHub PAT for publishing to weave-io/weave-agent-evals.
          # Scoped to this step only — not available in any other step.
          # The CLI reads this token and passes it ONLY as an HTTP Authorization
          # header. It never appears in shell command arguments or log output.
          EVAL_RESULTS_REPO_TOKEN: ${{ secrets.EVAL_RESULTS_REPO_TOKEN }}
          # Publish mode: activates GitHubContentsPublisher in the CLI.
          # Set to "local" to disable external publication (e.g. for dry runs).
          WEAVE_EVAL_PUBLISH_MODE: "publish"
          # Validated filter values — forwarded as env vars, never as argv.
          # Empty string means "no filter" (all agents / models / cases).
          WEAVE_EVAL_AGENT: ${{ needs.validate-inputs.outputs.agent }}
          WEAVE_EVAL_MODEL: ${{ needs.validate-inputs.outputs.model }}
          WEAVE_EVAL_MODELS: ${{ needs.validate-inputs.outputs.models }}
          WEAVE_EVAL_CASE:  ${{ needs.validate-inputs.outputs.case }}
          WEAVE_EVAL_REPEAT: ${{ needs.validate-inputs.outputs.repeat }}
          # Text-only cases only; see the dry-run step.
          WEAVE_EVAL_TRACK: "text"
          # CI=true prevents --raw-artifacts from being accepted (policy guard
          # in packages/cli/src/evals/input-validation.ts: RawArtifactsInCI).
          CI: "true"
          # Surface the run ID for metadata (safe: numeric only, no secrets).
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          set -euo pipefail
          # No filter flags in argv. The CLI reads WEAVE_EVAL_* from the env.
          # Bundle artifacts are written locally to eval-bundles/ in the workspace,
          # then published to weave-io/weave-agent-evals via the GitHub Contents API.
          bun packages/cli/src/main.ts eval run

      # -----------------------------------------------------------------------
      # Step 6: Upload local bundle artifacts (backup).
      #
      # Keeps a local copy of the bundle for inspection even when external
      # publication succeeds. This step uses no stored secrets and does not
      # perform any git operations.
      #
      # SECURITY:
      #   - No tokens are used in this step.
      #   - No git clone/push occurs.
      #   - All bundle content has already been sanitized by ArtifactBundleWriter
      #     (assertPublishSafe + assertJsonPublishSafe) before being written to disk.
      #   - The upload-artifact action only reads from the workspace path; it does
      #     not authenticate to any external service with a stored secret.
      #   - The raw/ subdirectory is excluded because upload-artifact reads only
      #     what the CLI wrote to eval-bundles/. The CLI never writes raw/ files
      #     when CI=true (--raw-artifacts is rejected by the CI guard).
      # -----------------------------------------------------------------------
      - name: Upload eval bundle artifacts
        # actions/upload-artifact v4.6.2
        # SHA pinned to the upstream commit for this tag — immune to tag mutation.
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        # always() ensures artifacts are uploaded even when the eval step exits
        # non-zero (e.g. one or more cases failed or publication failed).
        # Partial results are still useful for diagnosis and historical comparison.
        if: always()
        with:
          name: eval-bundles-${{ github.run_id }}
          # eval-bundles/ is the default bundle root written by EvalOrchestrator.
          # See EvalOrchestratorOptions.bundleRoot in packages/cli/src/evals/runner.ts.
          path: eval-bundles/
          # Retain artifacts for 30 days. Evals are expensive; keep enough history
          # for regression analysis without indefinite storage growth.
          retention-days: 30

  # ---------------------------------------------------------------------------
  # Job 3: Trajectory eval track (harness-mediated cases).
  #
  # This job runs cases whose expected_outcome.kind is "harness_trajectory" -
  # cases that need a real OpenCode harness session inside a sandbox container,
  # not just a text-only model completion. It is a separate track from
  # run-evals because it requires building and running a container image:
  # run-evals passes `--track text` and this job `--track trajectory`, so each
  # case runs in exactly one job.
  #
  # It runs on every manual dispatch (Spec 37, 20.2). It used to skip itself
  # unless trajectory-relevant paths had changed since the default branch,
  # which a dispatch from `main` never has, so it skipped every time. It now
  # skips only when the `trajectory` input is unticked or when the validated
  # filters leave it no work (see "Decide which eval jobs have work" in
  # validate-inputs). It honours the same agent, model, model-set, case and
  # repeat filters as run-evals.
  #
  # Cost: every trajectory case the filters select runs on every selected
  # model its `allowed_models` permits, one sandboxed session each (up to its
  # `max_duration_seconds`), times `repeat`.
  #
  # RISK (tracked from the plan's risks section):
  #   Podman on the GitHub-hosted ubuntu-latest runner is the initial sandbox
  #   backend target for this job. ubuntu-latest ships Podman but rootless
  #   container networking and cgroup v2 behavior on hosted runners is less
  #   battle-tested than a self-hosted runner. If Podman proves insufficient
  #   here (slow image builds, networking failures, resource limits), the
  #   fallback is to move this job to a self-hosted runner with a
  #   pre-provisioned container backend.
  # ---------------------------------------------------------------------------
  trajectory-evals:
    name: Run trajectory evals (harness track)
    runs-on: ubuntu-latest
    # Runs after run-evals, never beside it: both jobs publish, and a run ID
    # (`<sha7>-<date>-<NNN>`) is allocated by reading the published manifest,
    # so two concurrent publications could pick the same ID and the second
    # immutable upload would be rejected. `always()` keeps a skipped or
    # failed text job from suppressing the trajectory job; validate-inputs
    # must still have succeeded.
    needs: [validate-inputs, run-evals]
    if: always() && needs.validate-inputs.result == 'success' && needs.validate-inputs.outputs.run_trajectory == 'true'
    permissions:
      contents: read
    steps:
      # -----------------------------------------------------------------------
      # Step 1: Checkout source repo (needed for the harness sandbox build).
      # -----------------------------------------------------------------------
      - name: Checkout source repo
        # actions/checkout v4.2.2
        # SHA pinned to the upstream commit for this tag — immune to tag mutation.
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false

      # -----------------------------------------------------------------------
      # Step 2: Hardcoded sandbox profile allowlist (defense-in-depth).
      #
      # This job builds one sandbox image. Every sandbox_profile a trajectory
      # case names must be one it can serve, or the job fails closed here,
      # before any secret is in scope. workflow-sync.test.ts enforces that
      # ALLOWED_SANDBOX_PROFILES matches the profiles the cases reference.
      # -----------------------------------------------------------------------
      - name: Validate trajectory sandbox profiles
        run: |
          set -euo pipefail

          # Sandbox profile allowlist: exact expected_outcome.sandbox_profile
          # values referenced by trajectory cases.
          ALLOWED_SANDBOX_PROFILES="opencode-default opencode-local"

          PROFILES="$(jq -rs '[.[] | select(.expected_outcome.kind=="harness_trajectory") | .expected_outcome.sandbox_profile] | unique | .[]' evals/cases/*/*.json)"
          if [ -z "${PROFILES}" ]; then
            echo "::error::No harness_trajectory cases found under evals/cases"
            exit 1
          fi
          for profile in ${PROFILES}; do
            MATCHED=false
            for p in ${ALLOWED_SANDBOX_PROFILES}; do
              if [ "${profile}" = "${p}" ]; then MATCHED=true; break; fi
            done
            if [ "${MATCHED}" = "false" ]; then
              echo "::error::Sandbox profile '${profile}' not in ALLOWED_SANDBOX_PROFILES"
              exit 1
            fi
          done

          echo "Trajectory sandbox profile validation passed."

      # -----------------------------------------------------------------------
      # Step 3: Set up Bun runtime.
      # -----------------------------------------------------------------------
      - name: Set up Bun
        # oven-sh/setup-bun v2.2.0
        # SHA pinned to the upstream commit for this tag.
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: "1.3.10"

      # -----------------------------------------------------------------------
      # Step 4: Install dependencies.
      # -----------------------------------------------------------------------
      - name: Install dependencies
        run: bun install --frozen-lockfile

      # -----------------------------------------------------------------------
      # Step 5: Build the CLI.
      # -----------------------------------------------------------------------
      - name: Build
        run: bun run build

      # -----------------------------------------------------------------------
      # Step 6: Build the sandbox container image.
      #
      # See the RISK comment above the job: Podman on ubuntu-latest is the
      # initial target. If this step proves unreliable in practice, move the
      # job to a self-hosted runner with a pre-provisioned container backend.
      # -----------------------------------------------------------------------
      - name: Build sandbox image
        # Tag must match resolveSandboxProfileImage()'s mapping for the
        # "opencode-default" sandbox_profile in
        # packages/adapters/opencode/src/trajectory/opencode-trajectory-runner.ts
        # (SANDBOX_PROFILE_IMAGES). workflow-sync.test.ts enforces this stays
        # in sync with that source-of-truth mapping.
        run: podman build -t weave-sandbox-opencode-default sandboxes/opencode

      # -----------------------------------------------------------------------
      # Step 7: Dry-run the trajectory selection.
      #
      # No secrets. Validates the same filters the live step uses and fails
      # closed on an invalid combination before any model call.
      # -----------------------------------------------------------------------
      - name: Dry-run trajectory evals in CI env
        env:
          WEAVE_EVAL_PUBLISH_MODE: "publish"
          WEAVE_EVAL_AGENT: ${{ needs.validate-inputs.outputs.agent }}
          WEAVE_EVAL_MODEL: ${{ needs.validate-inputs.outputs.model }}
          WEAVE_EVAL_MODELS: ${{ needs.validate-inputs.outputs.models }}
          WEAVE_EVAL_CASE:  ${{ needs.validate-inputs.outputs.case }}
          WEAVE_EVAL_REPEAT: ${{ needs.validate-inputs.outputs.repeat }}
          WEAVE_EVAL_TRACK: "trajectory"
          CI: "true"
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          set -euo pipefail
          bun packages/cli/src/main.ts eval run --dry-run

      # -----------------------------------------------------------------------
      # Step 8: Run the trajectory evals and publish results.
      #
      # Uses the same publish contract as run-evals: OPENROUTER_API_KEY and
      # EVAL_RESULTS_REPO_TOKEN are scoped to this step only, and the CLI's
      # sanitizer pipeline (assertPublishSafe, assertJsonPublishSafe,
      # enforcePublishPolicy) runs before any external push to
      # weave-io/weave-agent-evals. Bundle shape and publisher target are
      # unchanged from the text-only track. Filters are forwarded as
      # WEAVE_EVAL_* env vars, never as argv.
      # -----------------------------------------------------------------------
      - name: Run trajectory evals
        env:
          # Secret: OpenRouter inference API key.
          # Scoped to this step only - not available in any other step.
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          # Secret: GitHub PAT for publishing to weave-io/weave-agent-evals.
          # Scoped to this step only - not available in any other step.
          EVAL_RESULTS_REPO_TOKEN: ${{ secrets.EVAL_RESULTS_REPO_TOKEN }}
          WEAVE_EVAL_PUBLISH_MODE: "publish"
          WEAVE_EVAL_AGENT: ${{ needs.validate-inputs.outputs.agent }}
          WEAVE_EVAL_MODEL: ${{ needs.validate-inputs.outputs.model }}
          WEAVE_EVAL_MODELS: ${{ needs.validate-inputs.outputs.models }}
          WEAVE_EVAL_CASE:  ${{ needs.validate-inputs.outputs.case }}
          WEAVE_EVAL_REPEAT: ${{ needs.validate-inputs.outputs.repeat }}
          WEAVE_EVAL_TRACK: "trajectory"
          CI: "true"
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          set -euo pipefail
          bun packages/cli/src/main.ts eval run

      # -----------------------------------------------------------------------
      # Step 9: Upload local bundle artifacts (backup).
      #
      # Same bundle shape and path as the text-only track's upload step
      # (eval-bundles/), and the same no-secrets, no-git-operations posture.
      # -----------------------------------------------------------------------
      - name: Upload trajectory eval bundle artifacts
        # actions/upload-artifact v4.6.2
        # SHA pinned to the upstream commit for this tag — immune to tag mutation.
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: trajectory-eval-bundles-${{ github.run_id }}
          path: eval-bundles/
          retention-days: 30
