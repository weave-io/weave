{
  "case_id": "pattern-plan-settings-refactor",
  "suite": "pattern-planning",
  "scoring": {
    "outcome_weight": 0.7,
    "per_expectation_weight": 0.3,
    "required": true,
    "notes": "Fairness/alignment: score only the structural planning artifacts the runner extracts, explicit scope, file-backed tasks, sequencing, and acceptance coverage. Do not require exact tag spellings like `#scope` or self-identification as Pattern."
  }
}
