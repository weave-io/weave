---
"@weaveio/weave-adapter-pi": patch
---

Ship a Pi extension bundle that loads on the pinned host without Bun's CommonJS bridge.

- The exact-host compatibility matrix and traced acceptance manifest state which host versions the adapter supports.
- Exhaustive closed-set evidence checks and a digest-bound live TUI smoke checklist back the manifest.
