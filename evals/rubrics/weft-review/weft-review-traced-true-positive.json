{
  "case_id": "weft-review-traced-true-positive",
  "suite": "weft-review",
  "scoring": {
    "outcome_weight": 0.7,
    "per_expectation_weight": 0.3,
    "required": true,
    "notes": "Judgment case: the verdict is withheld from the model. Score the [REJECT] verdict and whether at least one BLOCKER line is traced, i.e. cites two distinct code locations (the discarding call site and the fallible function it calls). A review whose blockers cite only the suspicious line is not traced."
  }
}
